# Traffic-Sign-Recognition-CNN
Traffic Sign Recognition using Deep Learning (CNN) This project focuses on classifying traffic signs using the GTSRB dataset (Kaggle). It involves preprocessing images (resizing, normalization), building a Convolutional Neural Network (CNN) model, and evaluating performance with accuracy and confusion matrix.
